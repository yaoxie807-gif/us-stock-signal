# 仅本地测试用；云端(GitHub Actions)用仓库 Secrets，不用这个文件。
# 复制成 .env 后，可本地 `python scan.py --selftest` 测试发送。
NTFY_TOPIC=
# 或者用完整 URL：
# NOTIFY_URL=https://ntfy.sh/你的topic

# Telegram 只作为可选备份。
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
